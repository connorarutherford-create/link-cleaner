# Link Cleaner — Browser Extension

Cleans tracking garbage from URLs. One click.

## Install (Developer Mode)

1. Download this repo as ZIP (or `git clone`)
2. Open **Chrome** → go to `chrome://extensions`
3. Toggle **Developer mode** (top right)
4. Click **Load unpacked**
5. Select the `link-cleaner` folder

## How to Use

Click the extension icon → it strips tracking parameters → click Copy.

Strips: `utm_source`, `fbclid`, `gclid`, `twclid`, `ref`, `aff_id`, and 30+ more.

## Pricing

$1 one-time on the Chrome Web Store (once published).

## Files

- `manifest.json` — extension config
- `popup.html` — the popup UI
- `popup.js` — tracking param stripper
- `icon16/48/128.png` — extension icons
